{
  "name": "vibecheck-cli",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run dev -w @evalit/cli",
    "dev": "npm run dev -w @evalit/cli",
    "build": "npm run build -w @evalit/shared && npm run build -w @evalit/cli",
    "start": "npm run start -w @evalit/cli"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
