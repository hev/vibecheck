metadata:
  name: invalid-suite
  # Missing required 'model' field

evals:
  - prompt: test prompt
    checks:
      match: "*test*"
      # Invalid semantic check - missing threshold
      semantic:
        expected: "test response"
