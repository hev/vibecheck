{
  "name": "vibecheck-cli",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run dev -w @evalit/cli",
    "dev": "npm run dev -w @evalit/cli",
    "build": "npm run build -w @evalit/cli",
    "build:link": "npm run build && npm link -w @evalit/cli",
    "start": "npm run start -w @evalit/cli"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "string-width": "^8.1.0"
  }
}
